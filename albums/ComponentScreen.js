import React from 'react';
import { Text, StyleSheet} from 'react-native';

const ComponentScreen = () => {
    return <Text>This is the components from PEPI</Text>
};

const styles = StyleSheet.create({
    textStyle:{
        fontSize:30
    }
});